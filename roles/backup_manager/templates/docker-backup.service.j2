[Unit]
Description=Backup Docker volumes
Wants=docker.service
After=docker.service

[Service]
Type=oneshot
User={{ main_user.name }}
Group={{ main_user.name }}
Environment=BACKUP_DIR={{ directories.backup }}
ExecStart={{ directories.scripts }}/docker_volumes_backup.sh
TimeoutStartSec={{ docker_backup.timeout }}
